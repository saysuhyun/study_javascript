/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어 (라틴문자)를 사용하며 문자와 숫자 모두 사용 가능 
 * 2) 특문은 _ , $ 만 사용 가능 
 * 3) 숫자로 시작 불가
 * 4) 키워드 변수명 불가
 * 
 */

let code = 'code';
var $ive = 'ive';
const _yuzin = 'yuzin';

console.log(code);
console.log($vie);
console.log(_yuzin);

/** 
 * 1) camelCase : 대부분 언어에서 많이 사용
 * 2) snake_case  : 파이썬 , 혹은 파일,폴더 이름에서 
 * 3) PascalCase : 그냥 잘 안 씀 
 */

const anYuJin = '';
let yu_zin;



